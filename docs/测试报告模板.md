# Paimon 性能测试报告

> **测试环境**：[填写环境名称，如 CDH 离线集群]  
> **测试阶段**：[阶段 1：极限压测 / 阶段 2：生产模拟负载]  
> **测试时间**：[开始时间] ~ [结束时间]  
> **测试人员**：[姓名]  
> **报告日期**：[YYYY-MM-DD]

---

## 一、测试概述

### 1.1 测试目标

- [ ] 验证 Paimon 主键表在流式写入场景（高频 I/U/D）的吞吐能力与稳定性
- [ ] 评估独立 Compaction 作业的合并效率与资源开销
- [ ] 测试流式读取与聚合的性能表现
- [ ] 验证并发读写场景下的相互影响与资源争抢
- [ ] 确认数据可见新鲜度符合 ≤5 分钟目标

### 1.2 测试配置

| 配置项 | 值 | 说明 |
|-------|---|------|
| 写入作业名 | `DataStreamperf_paimon` | |
| Compaction 作业名 | `paimon-compact` | crontab 周期 BATCH Minor Compact |
| 测试表名 | `wide_table` | 100 列宽表 |
| Bucket 数 | 512 | 按当前真实 Data Files 规模估算单 Bucket 约 1GB |
| 写入 Parallelism | 3 | 与 Kafka 3 分区对齐，不要求与 Bucket 数相等 |
| Checkpoint 周期 | 3 分钟 | |
| 数据生成速率 | [阶段 1: 不限速 / 阶段 2: 20000 rps] | |
| UPDATE/DELETE 比例 | ~50% | 主键复用历史值 |

### 1.3 测试数据规模

| 指标 | 值 |
|-----|---|
| 总写入记录数 | [XX,XXX,XXX] 条 |
| 测试运行时长 | [X] 小时 / [X] 天 |
| 峰值写入速率 | [XX,XXX] rps |
| 平均写入速率 | [XX,XXX] rps |

---

## 二、场景 1：单写入性能测试结果

### 2.1 核心指标达成情况

| 指标项 | 健康标准 | 实测值 | 达标情况 | 备注 |
|-------|---------|-------|---------|------|
| **写入吞吐** | 阶段 1: 探上限<br>阶段 2: ≥ 20000 rps | 峰值: [XX,XXX] rps<br>均值: [XX,XXX] rps<br>P99: [XX,XXX] rps | ✅ / ⚠️ / ❌ | |
| **反压持续时间** | 良好: < 10s<br>告警: > 30s | 触发次数: [X] 次<br>最大持续: [X] 秒<br>占比: [X]% | ✅ / ⚠️ / ❌ | |
| **Checkpoint 成功率** | > 99% | 成功: [XXX] 次<br>失败: [X] 次<br>成功率: [XX.X]% | ✅ / ⚠️ / ❌ | 若无此指标：观察快照推进 |
| **快照推进间隔** | 良好: ~3min<br>告警: > 5min | 均值: [X.X] 分钟<br>P99: [X.X] 分钟<br>最大: [X.X] 分钟 | ✅ / ⚠️ / ❌ | |

### 2.2 吞吐趋势图

```
[粘贴 StarRocks 导出的时序图，或描述趋势]
- 时间范围：[HH:MM] ~ [HH:MM]
- 趋势描述：[稳定上升 / 出现波动 / 稳定在 XX rps 等]
```

### 2.3 异常时段分析

#### 异常 1：[时间段] 反压持续

- **时间范围**：[HH:MM:SS] ~ [HH:MM:SS]（持续 [X] 分钟）
- **现象**：`checkpointStartDelayNanos` 持续 > 30 秒
- **根因**：[L0 堆积导致 / YARN 资源不足 / HDFS I/O 瓶颈 / 待分析]
- **影响**：写入吞吐下降至 [XX,XXX] rps（正常值 [XX,XXX] rps）
- **恢复**：[自动恢复 / 手动干预：增加 Compaction 资源]

#### 异常 2：[描述]
[按需补充]

### 2.4 结论

- ✅ **通过**：所有核心指标达标，无持续异常
- ⚠️ **有隐患**：[具体问题描述]，建议 [改进措施]
- ❌ **不通过**：[具体问题描述]，需 [整改措施]

---

## 三、场景 2：Compaction 性能测试结果

### 3.1 核心指标达成情况

| 指标项 | 健康标准 | 实测值 | 达标情况 | 备注 |
|-------|---------|-------|---------|------|
| **Compaction 繁忙度** | 良好: < 70%<br>告警: ≥ 90% | 均值: [XX]%<br>P99: [XX]%<br>峰值: [XX]% | ✅ / ⚠️ / ❌ | |
| **L0 文件堆积** | 稳定或缓慢增长 | 起始: [XX] 个<br>结束: [XX] 个<br>峰值: [XX] 个 | ✅ / ⚠️ / ❌ | 是否触发快速增长告警 |
| **Compaction 平均耗时** | 良好: < 30s<br>告警: > 60s | 均值: [XX.X] 秒<br>P99: [XX.X] 秒<br>峰值: [XX.X] 秒 | ✅ / ⚠️ / ❌ | |
| **Compaction 提交占比** | 根据 U/D 比例合理 | COMPACT: [XX]%<br>APPEND: [XX]%<br>其他: [XX]% | ✅ / ⚠️ / ❌ | 预期 ~50% COMPACT |

### 3.2 文件堆积趋势图

```
[粘贴 L0-L5 文件数时序图，或描述趋势]
- L0: [起始 XX → 结束 XX]
- L1-L5: [描述分布变化]
```

### 3.3 资源占用情况

| 资源项 | 均值 | 峰值 | 说明 |
|-------|-----|------|------|
| YARN CPU (vCores) | [XX] 核 | [XX] 核 | ⚠️ 集群级指标 |
| YARN 内存 (GB) | [XX] GB | [XX] GB | ⚠️ 集群级指标 |
| HDFS 存储增长 | [XX] GB/小时 | [XX] GB | 累计增长 [XX] GB |

### 3.4 异常时段分析

#### 异常 1：[时间段] L0 快速堆积

- **时间范围**：[HH:MM] ~ [HH:MM]
- **现象**：L0 从 [XX] 个快速增长至 [XX] 个（[X] 分钟内增长 [X] 倍）
- **根因**：[Compaction 繁忙度 > 90% / 写入速率激增 / 待分析]
- **影响**：触发 `health_flags.l0_flag = 'L0_PILEUP'`
- **恢复**：[自动恢复 / 手动增加 Compaction 并行度]

### 3.5 结论

- ✅ **通过**：Compaction 能跟上写入速率，文件数稳定
- ⚠️ **有隐患**：[具体问题]
- ❌ **不通过**：[具体问题]

---

## 四、场景 3：流式读取性能测试结果

> ⚠️ **状态**：[已完成 / 未完成（原因：待接入分析视图）]

### 4.1 核心指标达成情况

| 指标项 | 健康标准 | 实测值 | 达标情况 | 备注 |
|-------|---------|-------|---------|------|
| **流读吞吐** | ≥ 写入速率 | 均值: [XX,XXX] rps<br>峰值: [XX,XXX] rps | ✅ / ⚠️ / ❌ | 写入速率: [XX,XXX] rps |
| **Source 反压** | 良好: < 100 ms/s<br>告警: > 500 ms/s | 均值: [XX] ms/s<br>P99: [XX] ms/s | ✅ / ⚠️ / ❌ | 若无此指标：观察 busyTime |
| **数据可见延迟** | 良好: ≤ 3min<br>告警: > 5min | 均值: [X.X] 分钟<br>P99: [X.X] 分钟 | ✅ / ⚠️ / ❌ | |
| **快照停滞** | 不停滞 | 停滞次数: [X] 次<br>最长停滞: [X] 分钟 | ✅ / ⚠️ / ❌ | |

### 4.2 异常分析

[描述流读期间的异常，如消费滞后、Source 反压等]

### 4.3 结论

- ✅ **通过** / ⚠️ **有隐患** / ❌ **不通过** / **待测试**

---

## 五、场景 4：流式聚合性能测试结果

> ⚠️ **状态**：[已完成 / 未完成（原因：待接入分析视图）]

### 5.1 核心指标达成情况

| 指标项 | 健康标准 | 实测值 | 达标情况 | 备注 |
|-------|---------|-------|---------|------|
| **聚合输入吞吐** | ≥ 写入速率 | 均值: [XX,XXX] rps | ✅ / ⚠️ / ❌ | |
| **聚合输出频率** | 与输入同量级 | 均值: [XX,XXX] rps<br>放大比: [X.X]x | ✅ / ⚠️ / ❌ | |
| **算子繁忙度** | 良好: < 500 ms/s<br>告警: > 800 ms/s | 均值: [XX] ms/s<br>P99: [XX] ms/s | ✅ / ⚠️ / ❌ | |
| **状态大小** | KB 级别 | 均值: [XX] KB<br>峰值: [XX] KB | ✅ / ⚠️ / ❌ | 异常增长 = 状态退化 |

### 5.2 异常分析

[描述聚合期间的异常]

### 5.3 结论

- ✅ **通过** / ⚠️ **有隐患** / ❌ **不通过** / **待测试**

---

## 六、场景 5：并发读写测试结果

### 6.1 作业启动时间线

| 时间 | 事件 | 说明 |
|-----|------|------|
| [HH:MM] | 启动 Compaction 作业 | |
| [HH:MM] | 启动写入作业 | |
| [HH:MM] | 启动流式读作业 | 可选 |
| [HH:MM] | 启动流式聚合作业 | 可选 |

### 6.2 综合观测结果

#### 6.2.1 写入侧影响

- **吞吐变化**：并发前 [XX,XXX] rps → 并发中 [XX,XXX] rps（变化 [±X]%）
- **反压变化**：[是否增加 / 无明显变化]
- **结论**：✅ 无影响 / ⚠️ 轻微影响 / ❌ 严重影响

#### 6.2.2 合并侧影响

- **L0 堆积变化**：[描述并发前后 L0 文件数变化]
- **繁忙度变化**：并发前 [XX]% → 并发中 [XX]%
- **结论**：✅ 无影响 / ⚠️ 轻微影响 / ❌ 严重影响

#### 6.2.3 读取侧影响

- **流读滞后**：[是否出现滞后]
- **可见延迟变化**：[描述变化]
- **结论**：✅ 无影响 / ⚠️ 轻微影响 / ❌ 严重影响

#### 6.2.4 资源争抢情况

- **YARN CPU 使用率**：均值 [XX]%，峰值 [XX]%（是否饱和：[是 / 否]）
- **YARN 内存使用率**：均值 [XX]%，峰值 [XX]%（是否饱和：[是 / 否]）
- **HDFS I/O**：[是否成为瓶颈]
- **结论**：✅ 资源充足 / ⚠️ 资源紧张 / ❌ 资源瓶颈

### 6.3 健康标志综合视图

```sql
-- 查询结果示例（展示软标志触发情况；视图只有这三个软标志，见 05_health_flags.sql）
SELECT 
    time_bucket_minute,
    l0_flag,              -- OK / L0_PILEUP
    compaction_flag,      -- OK / COMPACTION_SATURATED
    backpressure_flag     -- OK / BACKPRESSURE
FROM RDW_DATA.health_flags
WHERE time_bucket_minute BETWEEN '<并发测试起>' AND '<并发测试止>'
  AND (l0_flag <> 'OK' OR compaction_flag <> 'OK'
       OR backpressure_flag <> 'OK')
ORDER BY time_bucket_minute;
```

| 时间段 | L0 堆积 | Compaction 饱和 | 反压 | 持续时长 |
|-------|--------|---------------|-----|---------|
| [HH:MM] ~ [HH:MM] | [OK / 告警] | [OK / 告警] | [OK / 告警] | [X] 分钟 |

**软标志触发统计**：
- L0_PILEUP: [X] 次，占比 [X]%
- COMPACTION_SATURATED: [X] 次，占比 [X]%
- BACKPRESSURE: [X] 次，占比 [X]%

（快照推进/停滞不在本视图，另查 `RDW_DATA.checkpoint_health`，见 `08_checkpoint_health.sql`）

### 6.4 结论

- ✅ **通过**：`health_flags` 全程 OK 或仅偶发告警（< 5%）
- ⚠️ **有隐患**：某维度持续告警但未影响核心指标
- ❌ **不通过**：出现作业失败、checkpoint 失败或严重性能下降

---

## 七、总体评估与建议

### 7.1 SLA 达成情况（阶段 2）

| SLA 指标 | 目标值 | 实测值 | 达标情况 |
|---------|-------|-------|---------|
| 写入吞吐 | ≥ 20000 rps | [XX,XXX] rps | ✅ / ❌ |
| 端到端延迟（数据可见新鲜度） | ≤ 5 分钟 | [X.X] 分钟 | ✅ / ❌ |
| 稳定性（连续运行） | 5-7 天无故障 | [X] 天，[X] 次故障 | ✅ / ❌ |

### 7.2 性能瓶颈识别

1. **写入侧瓶颈**：[是否存在 / 具体瓶颈描述]
2. **合并侧瓶颈**：[是否存在 / 具体瓶颈描述]
3. **资源瓶颈**：[CPU / 内存 / HDFS 存储 / 网络 I/O]
4. **配置瓶颈**：[checkpoint 周期 / parallelism / bucket 数等]

### 7.3 优化建议

#### 短期优化（1 周内可实施）

1. **[建议 1]**：[具体措施 + 预期效果]
2. **[建议 2]**：[具体措施 + 预期效果]

#### 长期优化（需要架构调整）

1. **[建议 1]**：[具体措施 + 预期效果]
2. **[建议 2]**：[具体措施 + 预期效果]

### 7.4 风险与限制

1. **[风险 1]**：[描述 + 缓解措施]
2. **[限制 1]**：[描述]（如：集群级资源指标无法拆分到单作业）

### 7.5 最终结论

- ✅ **通过**：所有场景达标，建议投入生产使用
- ⚠️ **条件通过**：部分场景有隐患，需实施 [优化措施] 后再投产
- ❌ **不通过**：存在严重性能问题，需 [整改措施] 后重测

---

## 八、附录

### 8.1 测试环境详情

| 组件 | 版本 | 配置 |
|-----|------|------|
| Flink | 1.19.2 | |
| Paimon | 1.1.1 | |
| Hadoop/HDFS | 3.3.6 | |
| Kafka | 2.8.1 | |
| StarRocks | [版本] | |
| YARN | [版本] | 总资源: [XX] vCores, [XX] GB 内存 |

### 8.2 作业配置详情

```json
// 写入作业 job-run-params.json
{
  "parallelism": 3,
  "checkpoint.interval": "3min",
  "table.exec.state.ttl": "0",
  // ... 其他配置
}
```

```bash
# Compaction 真实形态（2026-08-06 核实）：crontab 每 5 分钟提交一次 BATCH 模式的
# paimon-compact action（paimon-flink-action compact），跑完几十秒即退出，不是常驻流式作业。
# crontab 条目示例（具体命令行以现场为准）：
*/5 * * * * <提交 paimon-flink-action compact 的命令>
```

> 填写提示：批任务生命周期短于指标上报周期（3 分钟），compaction 作业侧指标
> （`metrics_compaction_job`、`compaction_flag`）在该形态下可能无数据；
> 写入/合并的分析口径见 [`写入与合并性能分析.md`](写入与合并性能分析.md)。

### 8.3 数据生成器配置

```properties
# data-generator.properties
rate.limit.enabled=false  # 阶段 1
# rate.limit.enabled=true   # 阶段 2
# rate.limit.rps=20000      # 阶段 2
update.ratio=0.5          # UPDATE 比例，0.5 = 更新/插入各半
account.total=30000000    # 账户总数（主键上限）
column.count=100          # 固定 100 列，与入湖 schema 一致，勿改
# 其余键（kafka.bootstrap / kafka.topic / ogg.*）见 scripts/conf/data-generator.properties.template
```

### 8.4 分析 SQL 版本

- `01_metrics_view.sql`：[执行时间 / 版本标识]
- `02_four_category_metrics.sql`：[执行时间]
- `05_health_flags.sql`：[执行时间]
- `08_checkpoint_health.sql`：[执行时间]

### 8.5 原始数据查询示例

```sql
-- 查询指标原始数据（供复核用）
SELECT 
    job_name,
    metric_name,
    CAST(metric_value AS DOUBLE) AS value,
    FROM_UNIXTIME(CAST(metric_ts AS BIGINT) / 1000) AS ts
FROM RDW_ODS_FLINK_METRICS
WHERE job_name IN ('DataStreamperf_paimon', 'compaction_job', 'wide_table', 'cluster')
  AND etl_dt BETWEEN '<起始日期>' AND '<结束日期>'
ORDER BY metric_ts;
```

---

**报告审核**：[审核人姓名]  
**审核日期**：[YYYY-MM-DD]  
**审核意见**：[通过 / 需补充 XX 信息]
